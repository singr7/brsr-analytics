# Edge config for a node that sits BEHIND another TLS terminator (omen).
#
# Differences from default.conf.template:
#   * plain HTTP on 8080 only - omen owns the certificate and the 80/443 redirect;
#   * no ACME webroot, no ssl_* directives, no self-signed bootstrap;
#   * real_ip from the trusted proxy, so the rate-limit zones meter the visitor
#     rather than metering omen as a single client.
#
# TRUSTED_PROXY_CIDR must be omen's address on the link it reaches this box on.

limit_req_zone $binary_remote_addr zone=public:10m  rate=120r/m;
limit_req_zone $binary_remote_addr zone=nlq:10m     rate=20r/m;
limit_req_zone $binary_remote_addr zone=uploads:10m rate=30r/m;
limit_req_status 429;
limit_conn_zone $binary_remote_addr zone=perip:10m;

upstream api_upstream {
  server api:8000 max_fails=3 fail_timeout=15s;
  keepalive 32;
}

server {
  listen 8080 default_server;
  server_name ${DOMAIN_NAME};

  set_real_ip_from ${TRUSTED_PROXY_CIDR};
  real_ip_header X-Forwarded-For;
  real_ip_recursive on;

  include /etc/nginx/security-headers.conf;

  server_tokens off;
  limit_conn perip 40;

  gzip on;
  gzip_vary on;
  gzip_min_length 1024;
  gzip_proxied any;
  gzip_types text/plain text/css text/javascript application/javascript application/json application/xml image/svg+xml;

  client_max_body_size 100m;
  client_body_timeout 120s;

  location = /nginx-health {
    access_log off;
    return 200 "ok\n";
  }

  location /healthz {
    proxy_pass http://api_upstream;
    include /etc/nginx/proxy-common.conf;
    access_log off;
  }

  location /api/nlq {
    limit_req zone=nlq burst=10 nodelay;
    proxy_pass http://api_upstream;
    include /etc/nginx/proxy-common.conf;
    proxy_read_timeout 120s;
  }

  location ~ ^/api/(studio|analysis)/ {
    limit_req zone=uploads burst=20 nodelay;
    proxy_pass http://api_upstream;
    include /etc/nginx/proxy-common.conf;
    proxy_read_timeout 300s;
    proxy_request_buffering off;
  }

  location /api/admin/ {
    include /etc/nginx/extra/admin-allowlist.conf;
    deny all;

    limit_req zone=public burst=40 nodelay;
    proxy_pass http://api_upstream;
    include /etc/nginx/proxy-common.conf;
  }

  location /api/ {
    limit_req zone=public burst=60 nodelay;
    proxy_pass http://api_upstream;
    include /etc/nginx/proxy-common.conf;
  }

  root /usr/share/nginx/html;
  index index.html;

  location /assets/ {
    include /etc/nginx/security-headers.conf;
    expires 1y;
    add_header Cache-Control "public, immutable" always;
    try_files $uri =404;
  }

  location / {
    include /etc/nginx/security-headers.conf;
    add_header Cache-Control "no-cache" always;
    try_files $uri $uri/index.html /index.html;
  }
}
